# Frontend environment variables
# In development, Vite proxies /api to the backend — set this to the backend URL
# In production, set to your domain (e.g. https://yourdomain.com)
VITE_API_URL=http://localhost:8000
